{
  "name": "autoshadow.desktop",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "postinstall": "ln -s ../../autoshadow node_modules; cd node_modules/autoshadow && npm install",
    "prepackage": "npm install",
    "package": "electron-packager . Autoshadow --platform=darwin --arch=x64 --version=1.2.3 --out=.. --overwrite"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-notifier": "^4.5.0"
  },
  "devDependencies": {
    "electron-packager": "^7.0.4"
  }
}
